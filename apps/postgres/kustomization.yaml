apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# postgres.yaml is raw manifests (Namespace, PVC, StatefulSet, Service) backing
# Authentik — NOT a Helm release despite the old pg_values.yml name. Adopted as
# a raw Kustomization like the other apps.
resources:
  - postgres.yaml
