apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# homepage-index.html is the source copy; the served HTML is embedded in the
# ConfigMap inside homepage.yaml, so only the manifest is applied.
resources:
  - homepage.yaml
