apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The served HTML is embedded in the ConfigMap inside homepage.yaml — edit it
# there. (The old homepage-index.html source copy was stale and removed.)
resources:
  - homepage.yaml
